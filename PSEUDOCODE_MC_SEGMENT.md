# 涡段云 + 蒙特卡洛远场 Biot-Savart 方法 — 伪代码

---

## 主循环

```
输入：初始涡段集合 S，刚体边界集合 B，时间步长 Δt，总时间 T
输出：演化后的涡段集合

初始化涡段（涡环/涡管/自定义几何）
在每个刚体表面采样边界点（按面积加权），记录法向量
计算所有段的几何缓存（中心、方向、长度）

对于每个时间步 t = 0, Δt, 2Δt, ..., T：

    ─── 第一步：边界处理（流固耦合） ───
    更新边界几何姿态（若刚体运动，则平移/旋转所有采样点和法向量）
    在每个边界采样点附近生成候选虚拟段（方向由法向量和来流决定）
    构建 K 矩阵：计算每个候选虚拟段对所有边界采样点的 Biot-Savart 诱导速度
    构建右端向量 b：b = u_boundary - u_内部段诱导 - u_背景（即边界上的速度缺口）
    求解正则化最小二乘：K·Γ = b，得到每个虚拟段的涡强 Γ
    将涡强足够大的虚拟段提交到全局段集合 S 中

    ─── 第二步：准备蒙特卡洛采样 ───
    计算自适应参考值 μ = 所有活跃段的 |γ|·L 的均值
    对每个活跃段，根据归一化重要性 ξ = |γ|·L / μ 随机增删样本标记
    收集样本索引集合；若为空则将全部段标为样本
    计算每个段中心的 Epanechnikov 核密度 f_hat
    计算每个段的近场邻域核密度之和 f_neigh

    ─── 第三步：计算端点速度（混合近场精确 + 远场蒙特卡洛） ───
    对每个活跃段 i（包含内部段和已提交的边界虚拟段）：
        初始化速度 = 背景速度
        近场：对所有距离 < r_near 的段 j，精确累加 Biot-Savart 诱导速度
        远场：仅对样本集中距离 ≥ r_near 的段 j，用 AKDE 权重累加并取平均

    ─── 第四步：RK4 时间积分 ───
    k1 = 当前端点速度（第三步已算好）
    k2 = 在 x + 0.5·Δt·k1 处重新评估速度（复用第二步的样本集）
    k3 = 在 x + 0.5·Δt·k2 处重新评估速度
    k4 = 在 x + Δt·k3 处重新评估速度
    更新端点位置：x_new = x + (Δt/6)(k1 + 2k2 + 2k3 + k4)
    更新涡强和年龄

    ─── 第五步：更新几何缓存 ───
    重新计算所有段的中心、方向、长度

    ─── 第六步：拓扑操作 ───
    分裂：将长度超过阈值的段从中点一分为二
    合并：将距离近且方向近反向的段对合并为一段（涡量向量守恒）
    删除：移除涡强绝对值低于阈值的弱段
    压缩段数组

结束
```
